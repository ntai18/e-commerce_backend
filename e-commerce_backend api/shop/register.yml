info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/shop/register"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
