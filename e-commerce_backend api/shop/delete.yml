info:
  name: delete
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/shop/update/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
