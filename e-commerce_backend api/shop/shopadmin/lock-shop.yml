info:
  name: lock-shop
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/shop/admin/lock/{account-id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
