info:
  name: me
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/address/me"
  headers:
    - name: Accept
      value: "*/*"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
