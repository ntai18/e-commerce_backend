info:
  name: productDetail
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/product/shop/detail/5"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
